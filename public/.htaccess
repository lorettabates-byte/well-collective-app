<IfModule mod_headers.c>
  <FilesMatch "\.(js|css|woff2?|ttf|svg|png|jpg|jpeg|gif|ico)$">
    Header set Cache-Control "public, max-age=31536000, immutable"
  </FilesMatch>
  # These two are unhashed filenames that change in place between builds
  # (unlike /assets/*.js, which get a new hash each build), so they must
  # be excluded from the long-cache rule above and always revalidated --
  # otherwise a browser that already cached sw.js can keep running a
  # stale service worker indefinitely, which is what made updates appear
  # to require deleting and re-adding the home-screen icon.
  <FilesMatch "^(index\.html|sw\.js|manifest\.webmanifest)$">
    Header set Cache-Control "no-cache, no-store, must-revalidate"
    Header set Pragma "no-cache"
    Header set Expires 0
  </FilesMatch>
</IfModule>

# Single-page app fallback: this is a React Router (client-side) app, so any
# URL that isn't an existing file/directory (e.g. /inspirations, /profile,
# a deep link from a push notification) must still serve index.html and let
# React Router handle it -- otherwise Apache 404s on direct navigation.
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  RewriteCond %{REQUEST_FILENAME} -f [OR]
  RewriteCond %{REQUEST_FILENAME} -d
  RewriteRule ^ - [L]
  RewriteRule ^ index.html [L]
</IfModule>
